{
  "name": "kintone-dev",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "deploy": "turbo run deploy",
    "release": "turbo run release",
    "generate": "node scripts/generate-app.mjs"
  },
  "devDependencies": {
    "prompts": "^2.4.2",
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.32.1"
}
